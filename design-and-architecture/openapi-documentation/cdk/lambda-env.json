{
  "Variables": {
    "BEDROCK_AGENT_ID": "${BEDROCK_AGENT_ID}",
    "MODEL_ID": "${MODEL_ID:-eu.anthropic.claude-3-7-sonnet-20250219-v1:0}",
    "BEDROCK_AGENT_ALIAS_ID": "${BEDROCK_AGENT_ALIAS_ID:-TSTALIASID}",
    "DOMAIN_ANALYZER_FUNCTION_ARN": "${DOMAIN_ANALYZER_LAMBDA_ARN}",
    "NODE_ENV": "${NODE_ENV:-production}",
    "BEDROCK_REGION": "${BEDROCK_REGION:-${AWS_REGION}}",
    "DOC_GENERATOR_FUNCTION_ARN": "${LAMBDA_FUNCTION_ARN}",
    "USER_POOL_CLIENT_ID": "${USER_POOL_CLIENT_ID}",
    "USER_POOL_ID": "${USER_POOL_ID}",
    "BUCKET_NAME": "${BUCKET_NAME}"
  }
}