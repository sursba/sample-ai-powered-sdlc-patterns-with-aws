# Project KB MCP Server - Environment Variables Example
# This file is for local development only
# For MCP deployment, use the generated mcp-config-template.json instead

# =============================================================================
# AWS COGNITO CONFIGURATION (Required)
# =============================================================================

# AWS Cognito User Pool ID - format: region_poolId
COGNITO_USER_POOL_ID=us-east-1_yourPoolId

# AWS Cognito App Client ID
COGNITO_CLIENT_ID=yourClientId

# AWS Cognito Identity Pool ID (for federated AWS credentials)
COGNITO_IDENTITY_POOL_ID=us-east-1:your-identity-pool-id

# Optional: Username for automatic authentication
COGNITO_USERNAME=your-username@example.com

# Optional: Password for automatic authentication
COGNITO_PASSWORD=your-secure-password

# =============================================================================
# BACKEND CONFIGURATION (Required)
# =============================================================================

# Default backend to use: 'opensearch' or 'bedrock'
DEFAULT_BACKEND=bedrock

# =============================================================================
# AWS BEDROCK CONFIGURATION (Required if DEFAULT_BACKEND=bedrock)
# =============================================================================

# Bedrock Knowledge Base ID
BEDROCK_KNOWLEDGE_BASE_ID=YOUR_KB_ID

# Bedrock region
BEDROCK_REGION=us-east-1

# =============================================================================
# AWS OPENSEARCH CONFIGURATION (Required if DEFAULT_BACKEND=opensearch)
# =============================================================================

# OpenSearch cluster endpoint URL
OPENSEARCH_ENDPOINT=https://your-collection.us-east-1.aoss.amazonaws.com

# OpenSearch collection name
OPENSEARCH_COLLECTION_NAME=your-collection-name

# =============================================================================
# AWS GENERAL CONFIGURATION
# =============================================================================

# AWS region for all services
AWS_REGION=us-east-1

# =============================================================================
# SERVER CONFIGURATION (Optional)
# =============================================================================

# Node.js environment
NODE_ENV=development

# Indicate this is running as an MCP server
MCP_SERVER=true