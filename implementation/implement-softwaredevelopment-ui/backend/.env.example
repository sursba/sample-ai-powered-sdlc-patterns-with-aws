# Cognito Configuration for S3 Access Control
COGNITO_USER_POOL_ID=your_user_pool_id
COGNITO_CLIENT_ID=your_client_id
COGNITO_IDENTITY_POOL_ID=your_identity_pool_id
COGNITO_REGION=us-east-1

# S3 Configuration
S3_BUCKET_NAME=your-s3-bucket-name
S3_REGION=us-east-1

# Authentication
AUTH_ENABLED=true

# AWS Region
AWS_REGION=us-east-1

# Amazon Q Business is now accessed directly via MCP with Cognito JWT from frontend

# External integrations removed - using direct Cognito authentication

# Server Configuration
HOST=0.0.0.0
PORT=8000