# CDK Deployment Configuration
# Copy this file to .env and customize for your deployment

# AWS Account Configuration
CDK_DEFAULT_ACCOUNT=your-aws-account-id
CDK_DEFAULT_REGION=us-east-1

# Additional AWS Account IDs (if you need to reference multiple accounts)
# ADDITIONAL_ACCOUNT_ID=your-additional-account-id

# Application Configuration
APP_NAME=icode

# Network Security (REQUIRED)
ALLOWED_IP_ADDRESS=1.2.3.4/32  # Replace with your IP address - use /32 for single IP

# SSL/HTTPS Configuration (REQUIRED)
# HTTPS is mandatory for security compliance
# 
# Certificate Options:
# 1. AWS Certificate Manager (ACM) - Request free SSL cert for your domain
# 2. Import existing certificate from any CA
#
# Domain can be from any registrar: Route 53, GoDaddy, Namecheap, Cloudflare, etc.
# Just ensure the certificate is imported into AWS ACM in the same region
CERTIFICATE_ARN=your-certificate-arn-here
DOMAIN_NAME=myapp.example.com  # Optional - your custom domain name

# Claude Model Configuration (REQUIRED)
CLAUDE_MODEL_ID=us.anthropic.claude-3-7-sonnet-20250219-v1:0

# ECR Configuration (optional - defaults to icode-fullstack:latest)
ECR_REPOSITORY_NAME=icode-fullstack
IMAGE_TAG=latest

# MCP Server Configuration (Optional - add your MCP server URLs)
# Format: comma-separated list of MCP server endpoint URLs
# Example: https://abc123.lambda-url.us-east-1.on.aws/,https://api.example.com/mcp
MCP_SERVER_URLS=

# Bedrock Configuration (Optional - set after creating Knowledge Base)
BEDROCK_KNOWLEDGE_BASE_ID=